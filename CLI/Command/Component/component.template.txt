import { createSignal } from "solid-js";
import { I{{ name }} } from "./types.ts";

export const {{ name }} = (props: I{{ name }}) => {
  const [count, setCount] = createSignal(1);

  return (
    <button onClick={() => setCount((count) => count + 1)}>
      count is {count()}
    </button>
  )
}
