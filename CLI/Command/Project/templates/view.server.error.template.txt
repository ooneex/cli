interface IServerErrorViewProps {
  name: string;
}

// TODO: refactor

const ServerErrorView = (props: IServerErrorViewProps) => {
  return (
    <div>
      ServerErrorView.tsx
    </div>
  );
};

export default ServerErrorView;
