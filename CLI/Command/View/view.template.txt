import { Island } from "@ooneex/island";
import { asset, Body, Head } from "@ooneex/view";

interface I{{ name }} {
  name: string;
}

const {{ name }} = (props: I{{ name }}) => {
  return (
    <>
      <Head title={"{{ name }}"} styles={[asset("{{ name }}.css")]} />
      <Body scripts={[asset("{{ name }}.js")]}>
        {{ name }}.tsx
      </Body>
      <Island name={"..."} />
    </>
  );
};

export default {{ name }};
